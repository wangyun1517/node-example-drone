debug: true
build:
  image: node
  commands:
    - npm install --development
    - npm test
deploy:
  ssh:
    host: 35.156.208.125
    user: ubuntu
    port: 22
    commands:
      - git -C /apps/node-example-drone pull origin master
      - pm2 delete all
      - pm2 start /apps/node-example-drone/index.js
